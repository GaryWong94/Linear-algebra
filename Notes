1 几何解释 矩阵解方程组的原理
  矩阵其实就是一种方程组 通过坐标系 可以看出来：矢向量的和就是方程组右边的结果列向量
  三个形如x+y+z=0的方程组 矩阵分解会变成 Ax=b 其中A矩阵是系数矩阵  x是参数vector b是结果vector  三个平面的交界为一个点
  把A分解变成 x*col1(A)+y*col2(A)+z*col3(A)=b   
  一个三维坐标系里 通过三个向量的组合，可以得到b，就是解，x y z的值就能得到
  线性相关：有一些向量没有贡献

2 用消元法可以算出解 行减行 相当于用其他代替y  if主元为0 行交换
  通过矩阵变换
